{
  "config": {
    "read_only": [
      {
        "description": "Set via environment variable",
        "key": "dns.listeningMode",
        "reason": "env_var"
      },
      {
        "description": "Set via environment variable",
        "key": "webserver.api.max_sessions",
        "reason": "env_var"
      },
      {
        "description": "Set via environment variable",
        "key": "webserver.api.password",
        "reason": "env_var"
      },
      {
        "description": "Variable can only be set in pihole.toml, not via API",
        "key": "misc.readOnly",
        "reason": "read_only"
      }
    ]
  },
  "took": 0.000022172927856445312
}
